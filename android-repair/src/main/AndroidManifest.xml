<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.kocomer.repair">

    <application>
        <activity android:name=".manager.activity.RepairConfigActivity"></activity>
        <activity android:name=".manager.activity.RepairRealActivity"></activity>
        <activity android:name=".manager.activity.RepairEngineerActivity"></activity>
        <activity android:name=".manager.activity.RepairTaskActivity"></activity>
    </application>
    <uses-permission android:name="android.permission.CAMERA"></uses-permission>

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
</manifest>
